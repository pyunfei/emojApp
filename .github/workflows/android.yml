name: 🚀 🚀 🚀 🚀 Build Android

on: 
  push:
    branches: [f-20211223-build-android]
  pull_request:
    branches: [master]

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [14.x]
    steps:
    - name: Checkout 
      uses: actions/checkout@v2

    - name: Make gradlew executable
      run: chmod +x  android/.gradlew

    - run:  yarn releaseApk
      working-directory: android
      name: Build Android apk (debug)

    - uses: actions/upload-artifact@v2
      with:
        name: android-apk
        path: '**/*.apk'